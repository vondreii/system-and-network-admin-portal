//-
//- deleteLabs.pug
//-
//- Project:   INFT2031 Network and Admin Portal 
//- Author:    UoN Major Project, 2016
//- Modified:  Sharlene Von Drehnen
//- Date:      10/12/2016 - 31/01/2017    
//- 

extends ../layout.pug

block title
	title Delete a Lab

block content
	script(src='../js/labpage.js')

	//- ----------------------- Mini navigation buttons to previous pages -------------------------------- -// 

	div#backButton

		img(src='/assets/img/arrow.gif')
		a(href='/lab')  Labs 
		img(src='/assets/img/arrow.gif')
		a(href='')  Delete a Lab
		
	//- --------------- Existing labs are displayed with the option to delete them ----------------------- -// 

	div#lab

		h1.o-header__title Delete a Lab
		p Choose the Lab/s you would like to delete
		
		div#labs-container
		script(src='../js/labpage.js')

		script.
			var obj = !{labinfo};
			var LabsIndex = obj.LabsIndex;
			var labsContainer = $('#labs-container');
			for (var i in LabsIndex) {

				var lab = $('<div>', {
					class: 'lab',
					html: LabsIndex[i].newlabname,

				});
				var button = $('<button>', {
					html: 'Delete',
					id: i


				});
				var link = $('<a>', {
					href: '#',

				});

				link.append(button);
				lab.append(link);
				lab.append("</a>");
				labsContainer.append(lab);
			}

		script.

			// Script to Delete Course - Does not work yet. 
			// Navigates to labpage.js

			/*
				var availableLabs = obj.availableLabs;
				for (var i in availableLabs){
					var number = String(i);
					document.getElementById(number).onclick=(function(opt){
						return function(){
							deleteLab(opt);
						}
					})(i)

				}
			*/

		br
		br




		

		